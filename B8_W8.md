# Web back end 8


## 1. Lập trình đồng bộ (Synchronous)
- Là cách lập trình mà:
  - Các lệnh chạy **tuần tự từ trên xuống dưới**
  - Lệnh sau phải **chờ lệnh trước hoàn thành**
- Đặc điểm:
  - ✔ Thứ tự rõ ràng  
  - ❌ Bị **chặn luồng (blocking)** nếu có tác vụ lâu  
  - ✔ Phù hợp tác vụ đơn giản  

👉 Ví dụ:
- Tính toán, vòng lặp → chạy lần lượt  
- Nếu có tác vụ lâu → chương trình bị đứng chờ  

---

## 2. Hàm ẩn danh (Anonymous Function)
- Là hàm **không có tên**
- Dùng để:
  - Gán vào biến  
  - Truyền làm tham số (callback)  
  - Viết ngắn gọn, dùng một lần  

### Cú pháp:
```js
function() { }
```

### Arrow function:
```js
() => { }
```

## 3. Cách sử dụng phổ biến
✔ Gán vào biến
```js
const cong = function(a, b) {
  return a + b;
}
```

✔ Callback
```js
setTimeout(function() {
  console.log("Hello");
}, 1000);
```

✔ Arrow function
```js
setTimeout(() => {
  console.log("Hello");
}, 1000);
```

✔ Trong mảng
```js
arr.forEach(num => console.log(num));
```

✔ IIFE (chạy ngay)
```js
(function() {
  console.log("Run ngay");
})();
```

## 4. Lưu ý

- Không có tên → khó debug

- Không hoisting

- Thường dùng cho code ngắn, không cần tái sử dụng

# 8.3 Bài tập

## Câu 8.2: Lập trình đồng bộ trong JavaScript là gì? Phát biểu nào sau đây không đúng?

(A.) Lập trình đồng bộ không chặn luồng, cho phép các lệnh tiếp theo chạy ngay cả khi tác vụ chưa hoàn tất.

B. Các lệnh được thực thi tuần tự, theo thứ tự từ trên xuống dưới.

C. Mỗi lệnh phải hoàn thành trước khi lệnh tiếp theo được thực thi.

D. Phù hợp với các tác vụ đơn giản như tính toán cơ bản không cần chờ đợi.

## Câu 8.3: Hàm ẩn danh (anonymous function) trong JavaScript là gì? Phát biểu nào sau đây không đúng?

A. Hàm ẩn danh có thể truy cập biến trong phạm vi bao quanh nhờ closure.

B. Hàm ẩn danh là hàm không có tên khi được định nghĩa.

C. Hàm ẩn danh thường được gán vào biến hoặc truyền làm tham số cho hàm khác.

(D.) Hàm ẩn danh được "nâng lên" (hoisted) giống như hàm khai báo (function declaration).
