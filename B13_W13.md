# Web back end 13

## 13. Lập trình client-server

---

## 13.1 Gửi request tới web server

### 🔹 Quy trình xử lý (7 bước)

1. Nhận request  
2. Phân tích request  
3. Đọc dữ liệu từ database  
4. Đưa dữ liệu vào HTML  
5. Gửi response về client  
6. Trình duyệt hiển thị  
7. Tải thêm tài nguyên tĩnh (CSS, JS, ảnh)

---

### 🔹 Quy trình đơn giản hóa

1. Server chạy và chờ request  
2. Client gửi request  
3. Server nhận request  
4. Server xử lý  
5. Server gửi response  
6. Client hiển thị kết quả  

---

### 🔹 Khởi động server

```js
'use strict'

const express = require('express')
const app = express();

const port = process.env.PORT || 9000

app.listen(port, () => {
    console.log(`server dang chay tren cong ${port}`);
});
```

### 🔹 Gửi request từ trình duyệt

Ví dụ URL:

http://localhost:9000/?name=shoes&size=40

### 🔹 Cấu trúc URL

protocol://domain:port/path?query#fragment

|Thành phần|Ý nghĩa|
|---------|--------|
|protocol|http / https|
|domain|localhost|
|port|9000|
|path|/|
|query|?name=shoes&size=40|
|fragment|#section|

### 🔹 Ví dụ phân tích

http://localhost:9000/?name=shoes&size=40


- Giao thức: http

- Domain: localhost

- Port: 9000

- Query:

  - name = shoes

  - size = 40

## 13.2 Gửi response về client

### 🔹 Xử lý GET request
```
app.get(path, callback)
```

- path: đường dẫn

- callback: xử lý request

### 🔹 Ví dụ hoàn chỉnh
```js
'use strict'

const express = require('express')
const app = express();

const port = process.env.PORT || 9000

app.get("/", (req, res) => {

    const product = req.query.name;
    const size = req.query.size;

    res.send(`Bạn muốn mua ${product} cỡ ${size}`);
});

app.listen(port, () => {
    console.log(`server dang chay tren cong ${port}`);
});
```

### 🔹 Giải thích

req.query → lấy dữ liệu từ URL

res.send() → gửi phản hồi về client

app.get() → xử lý request GET

### 🔹 Lưu ý

app.listen() luôn chạy trước

app.get() chỉ chạy khi có request

# 13.3 Bài tập

## Bài tập 13.1 Viết đoạn mã thực hiện yêu cầu sau:

Từ trình duyệt, người dùng gửi GET request, có kèm theo MSSV, HoTen, DiemTrungBinh của một sinh viên. Ví dụ:

MSSV:11223344

HoTen:Nguyễn Văn Tèo

DiemTrungBinh:7.0 

Web server nhận request trên, lấy thông tin từ request và gửi về client dòng thông báo có nội dung và định dạng như sau:

Chào bạn Nguyễn Văn Teo

MSSV: 11223344

Điểm trung bình của bạn là: 7.0

Chúc bạn học chăm!

```js
app.get("/", (req, res) => {

    const mssv = req.query.mssv;
    const hoten = req.query.hoten;
    const diem = req.query.diem;

    res.send(`
Chào bạn ${hoten}<br>
MSSV: ${mssv}<br>
Điểm trung bình của bạn là: ${diem}<br>
Chúc bạn học chăm!
    `);
});
```

Câu hỏi 13.2 URL (Uniform Resource Locator) là gì? Phát biểu nào sau đây không đúng?

A. URL là một địa chỉ duy nhất xác định vị trí của một tài nguyên trên Internet.

B. URL bao gồm các thành phần như giao thức, tên miền, đường dẫn, truy vấn và phân mảnh.

C. Phần "truy vấn" (query) trong URL được sử dụng để truyền dữ liệu đến máy chủ thông qua các tham số.

(D.) Giao thức "FTP" là giao thức phổ biến nhất được sử dụng trong URL để truy cập các trang web.
