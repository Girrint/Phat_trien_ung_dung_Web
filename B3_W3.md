# Web back end 3

## 1. Web server là gì?
- **Web server** là:
  - Một **máy tính (server)** hoặc **phần mềm**
  - Dùng để lưu trữ và cung cấp nội dung website

### 🔄 Cách hoạt động:
1. Trình duyệt gửi **request**
2. Web server nhận và xử lý
3. Trả về **response**
4. Trình duyệt hiển thị nội dung

---

## 2. Chức năng của Web server
- Lưu trữ các tập tin web (HTML, CSS, JS, hình ảnh…)
- Nhận request từ client
- Xử lý request (chạy code, truy vấn database…)
- Trả response về client

> ❗ Lưu ý: Web server **không nhận response từ client**

---

## 3. Quy trình xử lý
1. Nhận request  
2. Phân tích request  
3. Xử lý (có thể truy vấn database)  
4. Gắn dữ liệu vào HTML  
5. Trả response  
6. Trình duyệt hiển thị  
7. Trình duyệt tải thêm tài nguyên (CSS, JS, ảnh…)

---

## 4. Các web server phổ biến
- Apache  
- Nginx  
- NodeJS  
- IIS  
- Tomcat  
- Lighttpd  

> ❌ **SSI không phải web server**

---

## 5. Kiến trúc Web server
Một hệ thống web server gồm:
- Phần cứng server mạnh
- Hệ điều hành server
- Phần mềm web server (Apache, Nginx…)
- Có thể có:
  - Database (MySQL…)
  - Ngôn ngữ xử lý (PHP, Python…)

---

## 6. Nginx (ví dụ thực tế)

### ✔ Đặc điểm
- Mã nguồn mở
- Nhẹ, nhanh, phổ biến

### ✔ Cách chạy
- Giải nén → chạy `nginx.exe`

### ✔ Lệnh cơ bản
```bash
start nginx        # chạy
nginx -s stop      # dừng ngay
nginx -s quit      # dừng an toàn
nginx -s reload    # reload cấu hình
```

# ✅ Trả lời câu hỏi

## Câu 3.2: Phần mềm web server phổ biến – phát biểu sai?
A. Apache  
B. Nginx  
(C.) SSI  
D. Nodejs  
  
**SSI (Server Side Includes)** không phải là web server, mà chỉ là một kỹ thuật xử lý phía server.

---

## Câu 3.3: Chức năng của Web server – phát biểu sai?
A. Lưu trữ các tập tin của ứng dụng web  
B. Nhận request từ client  
C. Xử lý request  
(D.) Nhận response từ máy client  

- Web server chỉ **nhận request** và **trả response**, không nhận response từ client.
