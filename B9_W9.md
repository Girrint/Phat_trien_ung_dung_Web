# Web back end 9

## 9. Hàm mũi tên, lập trình bất đồng bộ trong JavaScript

**hàm mũi tên (arrow function)** và **lập trình bất đồng bộ**.

---

## 9.1 Hàm mũi tên (arrow function)

### 🔹 Khái niệm

Hàm mũi tên là một tính năng được giới thiệu trong **ES6 (ECMAScript 2015)**. Đây là cách viết hàm ngắn gọn và hiện đại trong JavaScript.

Hàm mũi tên sử dụng ký hiệu `=>` để định nghĩa hàm, giúp code ngắn gọn và dễ đọc hơn, đặc biệt trong callback và bất đồng bộ.

---

### 🔹 Đặc điểm

- Cú pháp ngắn gọn (bỏ `function`, có thể bỏ `{}` và `return`)
- Không có `this` riêng → kế thừa từ phạm vi bên ngoài (lexical scope)
- Không có `arguments`
- Không dùng làm constructor (`new`)

---

### 🔹 Cú pháp

```js
// Không tham số
() => { ... }

// Một tham số
x => { ... }

// Nhiều tham số
(x, y) => { ... }
```

🔹 Ví dụ: 
#### Không tham số
```js
const thongBao = () => {
    console.log("Chưa có thông báo, khi nào có thông báo sẽ thông báo!");
}

thongBao();

setTimeout(() => {
    console.log("Đã hết 4 giây! (sau 4 giây)");
}, 4000);

console.log("Đang chờ");
```

#### Một tham số
```js
const binhPhuong = x => x * x;
console.log(binhPhuong(3)); // 9

const numbers = [1, 2, 3, 4];
numbers.forEach(num => console.log(num * num));
```

#### Nhiều tham số
```js
const cong = (x, y) => x + y;
console.log(cong(2, 4)); // 6
```

### 🔹 Từ khóa this

Hàm mũi tên không có this riêng, mà kế thừa từ phạm vi bên ngoài:
```js
function Person() {
    this.age = 0;

    setInterval(() => {
        this.age++;
        console.log(this.age);
    }, 1000);
}

const p = new Person();
```

- Nếu dùng function() thì this sẽ bị sai ngữ cảnh.

### 🔹 Lưu ý

Không nên dùng arrow function khi:

- Cần arguments

- Cần dùng làm constructor

- Cần this riêng

## 9.2 Lập trình bất đồng bộ trong JavaScript

### 🔹 Lập trình đồng bộ (Synchronous)

- Các lệnh chạy tuần tự từ trên xuống

- Tác vụ lâu sẽ chặn chương trình

```js
function chanLuong() {
    console.log("Bắt đầu tác vụ cần nhiều thời gian xử lý");

    let batDau = Date.now();
    while (Date.now() - batDau < 5000) {
        // Giả lập chờ 5 giây
    }

    console.log("Tác vụ chạy 5 giây hoàn tất");
}

console.log("Trước khi chạy tác vụ");
chanLuong(); // chặn luồng 5 giây
console.log("Sau khi chạy tác vụ");
```

### 🔹 Lập trình bất đồng bộ (Asynchronous)

- Không chặn luồng chính

- Cho phép xử lý nhiều tác vụ cùng lúc

Phù hợp với:

- Gọi API

- Đọc/ghi file

- Database

### 🔹 Các kỹ thuật bất đồng bộ

#### Callback

- Là hàm truyền vào hàm khác

- Nhược điểm: dễ gây callback hell

```js
function layDuLieu(callback) {
    setTimeout(() => {
        callback("Dữ liệu đã nhận");
    }, 1000);
}
```

#### Promise

- Đại diện cho kết quả tương lai

- Dễ quản lý hơn callback

```js
let promise = new Promise((resolve, reject) => {
    resolve("Thành công");
});

promise
    .then(result => console.log(result))
    .catch(error => console.log(error));
```

#### Async/Await

- Viết code bất đồng bộ như đồng bộ

- Dễ đọc, dễ hiểu

```js
async function test() {
    let data = await Promise.resolve("Hello");
    console.log(data);
}
```

# 9.3 Bài tập

## Câu 9.2 Hàm mũi tên là gì? Phát biểu nào sau đây không đúng?

A. Hàm mũi tên có cú pháp ngắn gọn, giúp viết mã dễ đọc hơn.
(B.) Hàm mũi tên có this (ngữ cảnh) riêng, không kế thừa từ phạm vi bên ngoài.
C. Hàm mũi tên không thể được dùng làm hàm tạo (constructor).
D. Hàm mũi tên không có biến arguments để truy cập danh sách tham số.

## Câu 9.3 Lập trình bất đồng bộ là gì? Phát biểu nào sau đây không đúng?

A. Lập trình bất đồng bộ cho phép chương trình thực hiện nhiều tác vụ cùng lúc mà không cần chờ đợi tác vụ trước đó hoàn thành.
B. Lập trình bất đồng bộ đặc biệt hữu ích khi xử lý các tác vụ tốn thời gian như yêu cầu mạng, đọc/ghi tập tin hoặc tương tác với cơ sở dữ liệu.
(C.) Callback là một kỹ thuật bất đồng bộ, nó sẽ làm cho code dễ đọc và dễ bảo trì hơn khi code có nhiều callback lồng nhau.
D. Async/await cho phép viết mã bất đồng bộ trông giống như mã đồng bộ, giúp mã trở nên dễ đọc và dễ hiểu hơn.
