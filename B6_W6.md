# Web back end 6

## 1. package.json
- Là “hồ sơ ứng dụng” (manifest) trong Nodejs  
- Chứa:
  - Thông tin dự án (name, version…)  
  - Dependencies (thư viện sử dụng)  
  - Scripts (lệnh chạy)  
  - Entry point (file chạy đầu tiên, thường là `index.js`)  

### Tạo nhanh:
```bash
npm init -y
```

## 2. Cài đặt công cụ & thư viện
✔ Cài PNPM (quản lý package tối ưu hơn npm)
npm install -g pnpm
pnpm -v

✔ Ưu điểm PNPM

Lưu package 1 lần (tránh trùng lặp)

Dùng hard link → tiết kiệm dung lượng

Tối ưu giữa các version

✔ Cài Express (framework web)
pnpm i -s express


- Lưu trong node_modules

- Thêm vào dependencies trong package.json

## 3. Tạo Web Server (index.js)
'use strict'
```
const express = require('express')
const app = express()

const port = process.env.PORT || 9000

app.get("/", (req, res) => {
    res.send('Chao ban den voi TeoShop!!')
})

app.listen(port, () => {
    console.log(`server dang chay tren cong ${port}`)
})

Chạy server:
node index.js
```

-> http://localhost:9000

## 4. Nodemon

Tự restart server khi code thay đổi

Cài:
```npm install -g nodemon```

Chạy:
```nodemon```

## 5. Kiến thức quan trọng

node_modules: chứa thư viện

dependencies: thư viện chạy thật

devDependencies: thư viện phục vụ dev (test, tool…)

Global: dùng toàn hệ thống

Local: chỉ trong project

# 6.4 Bài tập

## Bài tập 6.2 Làm sao biết được một gói cài đặt ở chế độ cục bộ (locally) hay toàn cục (globally)? Khi cài đặt các gói (package) ở chế độ cục bộ và toàn cục (globally) thì các gói sẽ được lưu ở đâu? Làm sao bạn biết được?

## 📦 Phân biệt package cài Local vs Global

### 1. Cách biết cài Local hay Global

- **Local (cục bộ)**:
  - Cài bằng:
    ```bash
    npm install <ten-package>
    ```
    hoặc:
    ```bash
    pnpm add <ten-package>
    ```
  - 👉 Package sẽ xuất hiện trong:
    - `package.json` (dependencies hoặc devDependencies)
    - Thư mục `node_modules` của project

---

- **Global (toàn cục)**:
  - Cài bằng:
    ```bash
    npm install -g <ten-package>
    ```
  - 👉 Không nằm trong project cụ thể
  - Dùng được ở mọi nơi trong máy

---

### 2. Vị trí lưu trữ

- **Local**:
  - Lưu tại:
    ```
    <thu-muc-du-an>/node_modules/
    ```
  - Ví dụ:
    ```
    E:/TeoShop/node_modules/
    ```

---

- **Global**:
  - Lưu tại thư mục hệ thống của Nodejs:
    - Windows:
      ```
      C:\Users\<user>\AppData\Roaming\npm\node_modules
      ```
  - File chạy (.cmd) nằm ở:
    ```
    C:\Users\<user>\AppData\Roaming\npm\
    ```

---

### 3. Cách kiểm tra

#### ✔ Kiểm tra trong project
```bash
npm list
```

✔ Kiểm tra global
```npm list -g --depth=0```

✔ Xem vị trí cài đặt
```npm root```       # local
```npm root -g```    # global

✔ Tìm file thực thi
```where <ten-package>```

## Bài tập 6.3 Sự khác nhau khi cài đặt gói ở chế độ dependencies và devDependencies?

## 📦 Sự khác nhau giữa `dependencies` và `devDependencies`

### 1. dependencies
- Là các thư viện **cần thiết để chạy ứng dụng**
- Được sử dụng khi:
  - Ứng dụng chạy thật (production)
- Ví dụ:
  - express (tạo web server)
  - react, vue (frontend)

### ✔ Cài đặt:
```bash
npm install <ten-package>
# hoặc
pnpm add <ten-package>
```
👉 Sẽ được lưu vào:
```
"dependencies": {
  "express": "^4.21.2"
}
```

### 2. devDependencies

Là các thư viện chỉ dùng trong quá trình phát triển

Không cần khi deploy production

Ví dụ:

- nodemon (auto reload)

- jest (test)

- eslint (check code)

✔ Cài đặt:
```
npm install --save-dev <ten-package>
# hoặc
pnpm add -D <ten-package>
```
👉 Sẽ được lưu vào:
```
"devDependencies": {
  "nodemon": "^3.0.0"
}
```

## Câu 6.4 Trong ứng dụng chạy trên nền Nodejs, tập tin package.json dùng để làm gì? Phát biểu nào không đúng?

A. Chứa thông tin mô tả về ứng dụng

B. Quản lý các phụ thuộc

C. Chỉ ra tập tin JavaScript đầu tiên được thực thi

(D.) Chứa mã để tạo giao diện ứng dụng

## Câu 6.5 PNPM là gì? Phát biểu nào không đúng?

A. Giúp tiết kiệm dung lượng đĩa

B. Có thể thay thế hoặc bổ sung cho NPM

C. Là viết tắt của Performant Node Package Manager 

(D.) Không thể thay thế hoặc bổ sung cho NPM

## Câu 6.6 Express trong Node.js được sử dụng để làm gì?

(A.) Tạo ứng dụng web và API

B. Quản lý cơ sở dữ liệu

C. Thiết kế giao diện người dùng

D. Kiểm tra hiệu suất ứng dụng

## Câu 6.7 Nodemon trong Node.js được sử dụng để làm gì?

A. Kết nối với cơ sở dữ liệu

B. Tạo giao diện người dùng cho ứng dụng

C. Quản lý các gói phụ thuộc

(D.) Tự động khởi động lại server khi mã thay đổi
