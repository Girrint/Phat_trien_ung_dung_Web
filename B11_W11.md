# Web back end 11


## 11. Lập trình giao diện

Sau khi đã học về JavaScript, bất đồng bộ, callback, arrow function,... chúng ta bắt đầu làm việc với **giao diện (frontend)** và quản lý dự án.

---

## 11.1 Commit đầu tiên

### 🔹 Mục tiêu
- Quản lý mã nguồn bằng Git
- Tạo commit đầu tiên cho dự án **TeoShop**

---

### 🔹 Tạo file `.gitignore`

- Dùng để bỏ qua các file/thư mục không cần commit (ví dụ: `node_modules`)
- Có thể lấy mẫu tại: [Web](https://github.com/github/gitignore/blob/main/Node.gitignore)

### 🔹 Các lệnh Git

```bash
git status
git add .
git commit -m "Initialize TeoShop project with Express and Nodemon"
git log
```

#### 🔹 Lưu ý

Không commit thư mục node_modules

Nếu lỡ add rồi:
```
git rm -r --cached node_modules
```

## 11.2 Free HTML Template

### 🔹 Khái niệm

Free HTML Template là các mẫu giao diện web có sẵn gồm:

HTML

CSS

JavaScript

### 🔹 Khi nào nên dùng

Dự án nhỏ

Cần làm nhanh (prototype)

Ngân sách thấp

### 🔹 Cách sử dụng

Tải template từ:

[template](https://htmlcodex.com/)


Giải nén

Đổi tên thành public

Copy vào project

### 🔹 Ưu điểm

Tiết kiệm thời gian

Giao diện đẹp

Học được cách thiết kế

### 🔹 Nhược điểm

Không độc đáo

Dư code (CSS/JS)

Khó tùy chỉnh sâu

## 11.3 Static files trong Express

### 🔹 Khái niệm

Static files là:

HTML

CSS

JS

Hình ảnh

Font

👉 Không cần xử lý logic server

### 🔹 Cấu hình
```
app.use(express.static(__dirname + '/public'));
```

### 🔹 Giải thích

```app.use()``` → đăng ký middleware

```express.static()``` → phục vụ file tĩnh

```__dirname``` → đường dẫn thư mục hiện tại

```/public``` → thư mục chứa giao diện

### 🔹 Ý nghĩa

Truy cập:

http://localhost:9000/


👉 sẽ trả về file trong thư mục public (thường là index.html)

### 🔹 Mục đích

Tăng hiệu suất

Tổ chức project rõ ràng

Bảo mật tốt hơn

# 11.4 Bài tập

## Câu 11.2 Free HTML template là gì? Chọn phát biểu sai:

A. Là template miễn phí
B. Có giao diện đẹp
(C.) Tốn nhiều thời gian, phù hợp dự án lớn
D. Có thể tùy chỉnh

## Câu 11.3 Static files là gì? Chọn phát biểu sai:

A. Không cần xử lý logic server
B. Gửi qua express.static
C. Giúp tối ưu hiệu suất
(D.) Không nên đặt trong public
